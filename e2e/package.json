{
  "name": "document-editor-svelte-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Integration smoke tests that install the packed @onlyoffice/document-editor-svelte tarball into a real Svelte app and drive it with Playwright.",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "node scripts/setup.mjs && playwright test"
  },
  "devDependencies": {
    "@onlyoffice/doceditor-types": "^9.4.1",
    "@playwright/test": "^1.62.1",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
